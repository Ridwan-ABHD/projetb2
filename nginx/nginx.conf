server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    if ($request_uri ~ ^/(.+)\.html$) {
        return 301 /$1;
    }

    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
